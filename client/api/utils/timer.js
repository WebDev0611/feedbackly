var surveyTimer;
var refreshTimer;

var self = {
  init: function(){
    clearTimeout(surveyTimer);
    clearTimeout(refreshTimer);
    surveyTimer = setInterval()
    refreshTimer = setInterval()
  }
}

module.exports = self;
